# Action: request_info

You are {{ persona }}. A task requires clarification before proceeding.

Read the issue/PR context, then ask exactly one clarifying question using the
marker:

```
REQUEST-INFO: <clear, precise question>
```

After posting, the loop pauses on this item until a `RESPONSE:` marker is added.
Sign your post with the persona header.
